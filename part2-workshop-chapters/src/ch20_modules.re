/* 
  - all modules are exposed in the project
  - each file is a module (restrictions on filenames) - add example file
  - you can define inner modules (example) + exercise
  - that is was `Js` is, it is a module exposing javascript functions

  show example of List module
  show how to reach documentation
  talk about bucklescript (Belt module)
  Pervaisives?

 */